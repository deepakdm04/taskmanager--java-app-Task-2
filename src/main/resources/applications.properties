# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/taskdb

# Optional: Server port
server.port=8080

# Optional: Log SQL statements (for debugging)
spring.data.mongodb.logging.level=DEBUG
